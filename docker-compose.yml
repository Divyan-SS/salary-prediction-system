version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "10000:10000"
    volumes:
      - ./backend/dataset:/app/dataset
      - ./backend/models:/app/models
    environment:
      - PYTHONUNBUFFERED=1

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend